# 🚀 Быстрый старт

## Инструкция по запуску проекта

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

**Важно:** Первая установка может занять 5-10 минут, так как будут загружаться:
- PyTorch (~2 GB)
- Sentence Transformers
- Предобученные модели

---

### 2. Генерация обучающих данных (опционально)

```bash
python scripts/generate_data.py
```

Этот шаг создаст 300 синтетических примеров жалоб (по 15 на каждый рубрикатор).
Файл сохранится в `data/training_data.json`.

**Примечание:** Этот шаг не обязателен - классификатор работает без обучающих данных, используя только описания рубрикаторов.

---

### 3. Подготовка классификатора (обязательно!)

```bash
python scripts/train.py
```

Этот скрипт:
- Загрузит модель `paraphrase-multilingual-mpnet-base-v2` (~420 MB)
- Создаст векторные представления (эмбеддинги) всех 20 рубрикаторов
- Сохранит готовую модель в `models/classifier.pkl`

**Время выполнения:** ~2-5 минут при первом запуске (зависит от скорости интернета).

---

### 4. Тестирование классификатора

#### Вариант А: Тестирование на готовых примерах

```bash
python scripts/predict.py
# Выберите "1" для тестирования на примерах
```

#### Вариант Б: Интерактивный режим

```bash
python scripts/predict.py
# Выберите "2" и вводите свои жалобы
```

#### Вариант В: Классификация конкретного текста

```bash
python scripts/predict.py "Банк заблокировал мою карту без предупреждения"
```

---

## 📁 Структура проекта

```
rubrikator_api/
├── config/
│   ├── __init__.py
│   └── rubrics.py              # 20 рубрикаторов с ключевыми словами
│
├── src/
│   ├── __init__.py
│   ├── model.py                # Основной классификатор
│   └── preprocessor.py         # Утилиты обработки текста
│
├── scripts/
│   ├── __init__.py
│   ├── generate_data.py        # Генерация синтетических данных
│   ├── train.py                # Обучение/подготовка модели
│   └── predict.py              # Тестирование классификации
│
├── data/                       # Обучающие данные (создается автоматически)
├── models/                     # Сохраненные модели (создается автоматически)
├── requirements.txt
├── README.md
└── QUICKSTART.md              # Этот файл
```

---

## 🎯 Как это работает

### Гибридный подход:

1. **Sentence Transformers (70% веса)**
   - Модель: `paraphrase-multilingual-mpnet-base-v2`
   - Семантическое понимание текста
   - Работает с любыми формулировками

2. **Анализ ключевых слов (30% веса)**
   - Проверка наличия специфичных терминов
   - Уточняет категорию при спорных случаях
   - Повышает точность

### Процесс классификации:

```
Входящая жалоба
    ↓
Векторизация текста (Sentence Transformer)
    ↓
Сравнение с 20 рубрикаторами (cosine similarity)
    ↓
Анализ ключевых слов (TF-IDF подобный подход)
    ↓
Взвешенное комбинирование оценок (70%/30%)
    ↓
Топ-3 наиболее подходящих рубрикатора
```

---

## 💡 Примеры использования

### Пример 1: Блокировка карты

**Ввод:**
```
Банк Сбербанк заблокировал мою карту без объяснения причин. 
Операции ограничены согласно 161-ФЗ, но я не понимаю почему.
```

**Вывод:**
```
✅ ЛУЧШЕЕ СОВПАДЕНИЕ: Жалоба на блокировку карты/счета
   Уверенность: 92%
```

### Пример 2: Коллекторы

**Ввод:**
```
Мне звонят коллекторы по 30 раз в день по просроченному кредиту в МФО. 
Угрожают, названивают родственникам. Это незаконно!
```

**Вывод:**
```
✅ ЛУЧШЕЕ СОВПАДЕНИЕ: Жалоба на коллекторов
   Уверенность: 95%
```

### Пример 3: Мошенничество

**Ввод:**
```
Позвонили якобы из Росфинмониторинга, требуют перевести деньги 
на безопасный счет. Прошу проверить, работает ли это лицо у вас.
```

**Вывод:**
```
✅ ЛУЧШЕЕ СОВПАДЕНИЕ: Проверка сотрудника Росфинмониторинга
   Уверенность: 89%
```

---

## 🔧 Настройка параметров

Вы можете изменить параметры классификатора в [scripts/train.py](scripts/train.py):

```python
classifier = ComplaintClassifier(
    model_name="paraphrase-multilingual-mpnet-base-v2",  # Модель
    use_keywords=True,      # Использовать ключевые слова
    keyword_weight=0.3      # Вес ключевых слов (0.0 - 1.0)
)
```

**Рекомендуемые значения:**
- `keyword_weight=0.3` - универсальный вариант (по умолчанию)
- `keyword_weight=0.5` - больше внимания ключевым словам
- `keyword_weight=0.1` - больше доверия семантике

---

## 📊 Ожидаемая точность

| Метод | Точность |
|-------|----------|
| Только Sentence Transformers | 75-85% |
| + Ключевые слова | **85-92%** |
| + Fine-tuning на реальных данных | 92-97% |

---

## 🐛 Устранение неполадок

### Ошибка: "No module named 'torch'"
```bash
pip install torch==2.1.2
```

### Ошибка: "Model not found"
```bash
# Убедитесь, что запустили обучение
python scripts/train.py
```

### Медленная работа
- Первый запуск медленнее (загрузка модели в память)
- Последующие предсказания быстрые (~50-100ms на жалобу)

---

## 🚀 Следующие шаги

После тестирования базовой версии можно:

1. **Добавить REST API (FastAPI)**
   ```bash
   pip install fastapi uvicorn
   # Создать api/main.py с эндпоинтами
   ```

2. **Подключить базу данных (PostgreSQL)**
   ```bash
   pip install psycopg2-binary sqlalchemy
   # Сохранять историю классификаций
   ```

3. **Собрать реальные данные**
   - Логировать все классификации
   - Размечать вручную для точности
   - Дообучить модель на реальных примерах

4. **Continuous Learning**
   - Периодически обновлять эмбеддинги
   - Добавлять новые примеры в обучающую выборку

---

## 📞 Поддержка

При возникновении проблем проверьте:
1. Версию Python (требуется 3.8+)
2. Установлены ли все зависимости
3. Достаточно ли места на диске (~3 GB для моделей)
4. Запущено ли обучение (`python scripts/train.py`)

---

**Удачи в работе с классификатором! 🎉**
