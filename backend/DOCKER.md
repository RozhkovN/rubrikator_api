# 🐳 Docker Deployment

## Быстрый старт с Docker

### Предварительные требования

1. **Установите Docker и Docker Compose:**
   - [Docker Desktop для Mac](https://docs.docker.com/desktop/install/mac-install/)
   - Docker Compose обычно включен в Docker Desktop

2. **Обучите модель (обязательно!):**
   ```bash
   python scripts/train.py
   ```
   Модель должна быть в `models/classifier.pkl`

---

### Вариант 1: Использование скрипта (рекомендуется)

```bash
# Сделать скрипт исполняемым
chmod +x docker.sh

# Запустить
./docker.sh
```

Скрипт предложит меню с опциями.

---

### Вариант 2: Docker Compose команды

#### Запустить контейнер
```bash
docker-compose up -d
```

#### Остановить контейнер
```bash
docker-compose down
```

#### Перезапустить
```bash
docker-compose restart
```

#### Просмотр логов
```bash
docker-compose logs -f
```

#### Пересборка образа
```bash
docker-compose build --no-cache
docker-compose up -d
```

---

### Проверка работы

После запуска API будет доступен на:
- **API:** http://localhost:8800
- **Документация:** http://localhost:8800/docs
- **Health check:** http://localhost:8800/health

**Тест через curl:**
```bash
curl -X POST "http://localhost:8800/classify" \
  -H "Content-Type: application/json" \
  -d '{"text": "Банк заблокировал мою карту без предупреждения"}'
```

---

### Структура Docker

**Dockerfile:**
- Базовый образ: Python 3.11-slim
- Устанавливает все зависимости
- Копирует код приложения
- Открывает порт 8800

**docker-compose.yml:**
- Автоматически собирает образ
- Монтирует volumes для моделей и данных
- Настраивает healthcheck
- Автоматический перезапуск

**Volumes:**
- `./models:/app/models` - сохранение моделей
- `./data:/app/data` - сохранение данных

---

### Советы по разработке

**Режим разработки с hot-reload:**

Раскомментируйте в `docker-compose.yml`:
```yaml
volumes:
  - ./api:/app/api
  - ./src:/app/src
  - ./config:/app/config
```

Теперь изменения в коде будут применяться автоматически.

**Запуск команд внутри контейнера:**
```bash
docker-compose exec rubrikator-api python scripts/predict.py
```

---

### Troubleshooting

**Проблема:** Контейнер не запускается
```bash
# Проверить логи
docker-compose logs

# Проверить статус
docker-compose ps
```

**Проблема:** Модель не найдена
```bash
# Убедитесь, что модель обучена
ls -la models/classifier.pkl

# Если нет, обучите
python scripts/train.py
```

**Проблема:** Порт занят
```bash
# Проверить, что использует порт 8800
lsof -i :8800

# Изменить порт в docker-compose.yml
ports:
  - "9000:8800"  # Внешний:Внутренний
```

---

### Production deployment

Для продакшена рекомендуется:

1. **Использовать gunicorn:**
   ```dockerfile
   CMD ["gunicorn", "-w", "4", "-k", "uvicorn.workers.UvicornWorker", "api.main:app", "--bind", "0.0.0.0:8800"]
   ```

2. **Добавить nginx как reverse proxy**

3. **Настроить логирование**

4. **Добавить мониторинг (Prometheus + Grafana)**

---

## Основные команды

| Команда | Описание |
|---------|----------|
| `docker-compose up -d` | Запустить в фоновом режиме |
| `docker-compose down` | Остановить и удалить контейнер |
| `docker-compose restart` | Перезапустить |
| `docker-compose logs -f` | Просмотр логов |
| `docker-compose ps` | Статус контейнеров |
| `docker-compose build` | Пересобрать образ |

---

**Готово! Контейнер настроен и готов к работе.** 🚀
